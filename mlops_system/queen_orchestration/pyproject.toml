[project]
name = "queen-orchestration"
version = "1.0.0"
description = "Pipeline orchestration and tracking for the MLOps RPi cluster"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115.0",
    "uvicorn[standard]>=0.30.0",
    "apscheduler>=3.10.0",
    "paramiko>=3.4.0",
    "httpx>=0.27.0",
    "pyyaml>=6.0",
]
