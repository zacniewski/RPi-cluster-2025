[project]
name = "django-rpi-cluster"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "django>=5.2.4",
    "psutil>=7.0.0",
    "python-decouple>=3.8",
]
